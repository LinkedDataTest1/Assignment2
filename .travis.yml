sudo: required
dist: trusty

language: true

before_install:
  - chmod +x ./build/testAssignment2.sh
  - chmod +x .api

install: true
  
script:
  - ./build/testAssignment2.sh > err
  
after_success:
  - ./.api true
  
after_failure:
  - ./.api false
  
