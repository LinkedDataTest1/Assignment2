if [ "$1" == "true" ]
then
  msg="Build Succesful"
else
  msg=$(sed ':a;N;$!ba;s/\n/<br\/>/g' err)
fi
echo "https://api.github.com/repos/${TRAVIS_REPO_SLUG}/issues/${TRAVIS_PULL_REQUEST}/comments"
sleep 1
curl -H "Authorization: token ${TOKEN}" -X POST -d "{\"body\": \" $msg \"}" "https://api.github.com/repos/${TRAVIS_REPO_SLUG}/issues/${TRAVIS_PULL_REQUEST}/comments"
