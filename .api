if [ "$1" == "true" ]
then
  msg="Build Succesful"
else
  msg=$(sed ':a;N;$!ba;s/\n/<br\/>/g' err)
fi
sleep 1
if [ "$TRAVIS_PULL_REQUEST" = "false" ]
then
  curl -v -H "Authorization: token ${TOKEN}" -X POST -d "{\"body\": \" "algo" \"}" "https://api.github.com/repos/${TRAVIS_REPO_SLUG}/issues/${TRAVIS_PULL_REQUEST}/comments"
fi
