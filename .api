if [ "$1" == "true" ]
then
  msg="Build Succesful"
else
  msg=$(sed ':a;N;$!ba;s/\n/<br\/>/g' err)
fi
sleep 1
curl -v -H "Authorization: token ${TOKEN1}${TOKEN2}" -X POST -d "{\"body\": \" "$msg" \"}" "https://api.github.com/repos/${TRAVIS_REPO_SLUG}/issues/${TRAVIS_PULL_REQUEST}/comments"
